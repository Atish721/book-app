<%
bookCoverClass = locals.large ? 'book-cover-large' : ''
bookGridClass = locals.large ? 'book-grid-large' : ''
%>
<div ckass='book-grid <%=bookGridClass%>'>
    <%
    books.forEach(book=>{
    %>
    <a href='/books/<%=book.id%>'>
        <img class='book-cover <%=bookCoverClass%>' height='150' widht='100' src='<%=book.coverImagePath%>'>
    </a>
    <%
    })
    %>

</div>
